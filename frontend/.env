# Mock ativo por padrão (não precisa do backend). CPF de teste: 123.456.789-01
VITE_USE_MOCK=true

# URL da API do CRM (usado quando VITE_USE_MOCK=false)
# Em desenvolvimento (backend local Flask):
VITE_API_URL=/api

# Em produção (API real do CRM da OAB), trocar para:
# VITE_API_URL=https://api.oab.org.br/v1
